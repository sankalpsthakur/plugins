{
  "preCommand": [
    {
      "name": "validate-calc-prereqs",
      "description": "Ensure compute commands run only when required data artifacts exist."
    }
  ],
  "postCommand": [
    {
      "name": "record-calc-artifacts",
      "description": "Capture summary, traceability, and hotspots outputs for audit packets."
    }
  ]
}
