{
  "preCommand": [
    {
      "name": "validate-exec-prereqs",
      "description": "Ensure lock state and storage prerequisites before pipeline or OCR commands."
    }
  ],
  "postCommand": [
    {
      "name": "record-exec-artifacts",
      "description": "Capture pipeline, OCR, anomaly, and audit artifacts after execution runs."
    }
  ]
}
